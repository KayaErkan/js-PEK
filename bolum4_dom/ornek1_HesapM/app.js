function calcNumbers(result) {
  form.displayResult.value = form.displayResult.value + result;
}
document.querySelector(".button light Cey").onClick = function () {
  document.querySelector(".ekranBorder").value = "";
};
